const ParentComponent = () => {
    //STEP 2: Create a `data` object with name, age and city keys
    
    return (
    <div style={{ border: '1px solid black', padding: '10px' }}>
        <h1>Parent Component</h1>
        {/* STEP 3: Create a <ChildComponent /> and pass the `data` object as a prop*/}

        {/* FOR CONTEXT - STEP 4: Create a <ChildComponent /> */}
    </div>
    );
}

export default ParentComponent;