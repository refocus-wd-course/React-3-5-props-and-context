import './App.css';

// FOR CONTEXT - STEP 2: Create a MyContext File

function App() {
  // FOR CONTEXT - STEP 1: Create a `data` object with name, age and city keys
  return (
    <div className="App">
        <h1>App Component</h1>
        {/* STEP 1: Create a Parent Component */}
        
        {/* FOR CONTEXT - STEP 3: Create a MyContext.Provider, pass the `data` object and wrap the parent component */}
    </div>
  );
}

export default App;